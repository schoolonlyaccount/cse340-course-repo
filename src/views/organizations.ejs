<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <section>
            <h2>Here are a few of our partner organizations:</h2>
            <ul>
                <% organizations.forEach(organization=> { %>
                    <li><img src="/images/<%= organization.logo_filename %>" alt="<%= organization.name %> logo"><a
                            href="/organization/<%= organization.organization_id %>"><%= organization.name %></a>: <%= organization.contact_email %>
                    </li>
                    <% }); %>
            </ul>
        </section>
        <% if (user && user.role_name === 'admin') { %>
            <p><a href="/new-organization">Add a new organization</a></p>
        <% } %>
    </main>
    <%- include('partials/footer') %>