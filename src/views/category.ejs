<%- include('partials/header') %>
    <main>
        <h1>
            <%= title %>
        </h1>
        <section>
            <h2>
                <%= categoryDetails.name %>
            </h2>
            <p>Associated Service Projects:</p>
            <ul>
                <% serviceProjects.forEach(project=> { %>
                    <li><a href="/project/<%= project.project_id %>">
                            <%= project.title %>
                        </a></li>
                    <% }) %>
            </ul>
        </section>
    </main>
    <%- include('partials/footer') %>